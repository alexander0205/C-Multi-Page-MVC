@model PageWebMic.Areas.Admin.Models.articulos_contenido

<h2>@Model.id_title</h2>
<br />

<span>

    <button href="@Url.Action("contenido","Home",new { @area ="media",id= (int)ViewData["padrePrincipalId"]})" class="btn btn-default mediaColorBox">
        <span class="glyphicon glyphicon-picture" aria-hidden="true">
        </span>
        Media
    </button>

</span>
<span  style="float: right;">
    <a href="@Url.Action("Create",new { id=Model.id, padrePrincipalId = (int)ViewData["padrePrincipalId"]})" class="btn btn-default" type="submit">
        <span class="glyphicon glyphicon-folder-open" aria-hidden="true">
        </span>
         Crear Sub
    </a>
    <a href="@Url.Action("Edit",new { id=Model.id, padrePrincipalId = (int)ViewData["padrePrincipalId"]})" class="btn btn-default" type="submit">
        <span class="glyphicon glyphicon-pencil" aria-hidden="true">
        </span>
       Actualizar
    </a>
</span>

<br />
<br />
<br />